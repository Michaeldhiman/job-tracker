# =========================================================================
# Snap Job Frontend Environment Variables Template
# Copy this file to '.env' and fill in your custom credentials.
# =========================================================================

# --- API Endpoint Connections ---
# Base API URL pointing to your backend Express server (used by Axios Client requests)
# Leave empty only when the frontend is served from the same origin as the backend API.
VITE_API_BASE_URL=http://localhost:5000
